spacecraft: SMAP
exclude_channels:
  - P-2

window_size: 50
stride: 1

rolling_window: 100
n_sigma: 3.0

isolation_forest:
  n_estimators: 200
  max_samples: auto
  contamination: auto
  random_state: 42
  n_jobs: -1

paths:
  data_dir: data
  artifacts_dir: artifacts
  labels_file: data/labeled_anomalies.csv
  train_dir: data/train
  test_dir: data/test
